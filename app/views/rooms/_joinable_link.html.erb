<% if current_user.has_joined_room(room) %>
    <%= link_to "Leave", leave_room_path(room), data: { turbo: 'false' } %>
<% else %>
    <%= link_to "Join", join_room_path(room), data: { turbo: 'false' } %>
<% end %>