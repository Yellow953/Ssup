<% rooms.each do |room| %>
    <div class="flex bg-white p-2 justify-between">
        <%= link_to room.name, room_path(id: room.id, name_search: params[:name_search]), data: { turbo: 'false' }%>
        <%= render "rooms/joinable_link", room: room %>
    </div>    
<% end %>
